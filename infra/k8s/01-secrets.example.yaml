# Copy to 01-secrets.yaml (gitignored), fill in real values, and apply — never commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: nagar-seva-secrets
  namespace: nagar-seva
type: Opaque
stringData:
  JWT_SECRET: change-me-to-a-long-random-string
  MONGODB_URI: mongodb://mongo:27017/nagar_seva

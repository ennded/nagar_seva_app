# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

(Pinned to SDK 54, NOT the npm "latest" tag (57 as of writing). The Play Store apparently serves an
older Expo Go build on this developer's Android device/version — its in-app "About" screen reports
support for SDK 54 only, confirmed by testing 57 and 56 and getting "project is incompatible /
requires a newer version of Expo Go" both times. Do not chase npm's "latest" tag here — if the
incompatible-version error recurs, first ask the user to check Expo Go's own About screen for the
SDK number it actually supports, then match the project to THAT number, not to npm latest.)
